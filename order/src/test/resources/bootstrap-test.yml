spring:
  data:
    rest:
      basePath: /api
  sleuth:
    enabled: false
    stream:
      enabled: false

  cloud:
    config:
      enabled: false
      discovery:
        enabled: false
        service-id: config-service
